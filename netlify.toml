[build]
  command = "vite build"
  publish = "dist"

# SPA fallback - serve index.html for all routes
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[dev]
  command = "vite dev"
  port = 3000